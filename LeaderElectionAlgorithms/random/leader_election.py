import numpy as np

def leader_election(aircraft):
    # test = np.random.normal(loc = aircraft.positions, scale=2.25, size = aircraft.positions.shape)
    # print(np.mean(np.abs(test-aircraft.positions), axis=0))
    pass